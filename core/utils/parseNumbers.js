module.exports = (array, count) => {
    let output = [];

    for (let i = 0; i < count; i++) {
        let x = Number(element);
        if (isNaN(x)) return false;
        output.push(x);
    }

    return output;
}