/*screen = blessed.screen({
    //dump: __dirname + '/logs/layout.log',
    smartCSR: true,
    autoPadding: true,
    //warnings: true
});

let menuBar = blessed.box({
    parent: screen,
    tags : true,
    content : '{red-fg}{green-bg}{bold}hello world{/bold}{/green-bg}{/red-fg}'
})

let layout = blessed.layout({
    parent: screen,
    top: 1,
    left: 'center',
    width: '100%',
    height: '100%-1',
    layout: 'inline',
    style: {
        //bg: 'red'
    }
});

let box1 = blessed.box({
    parent: layout,
    top: 'center',
    left: 'center',
    width: 20,
    height: 10,
    border: 'line',
    label : "some random mf box",
    content: '1'
});

layout.hide();
*/
/*screen.key(['escape', 'q', 'C-c'], function() {
    return screen.destroy();
});*/

//screen.render();

//const term = require('terminal-kit').terminal;

//const blessed = require("neo-blessed");